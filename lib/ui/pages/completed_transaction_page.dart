import 'package:flutter/material.dart';


class CompletedTransactionPage extends StatelessWidget {
  const CompletedTransactionPage({super.key, required this.orderId});

  final String orderId;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body : Container() ,
    );
  }
}